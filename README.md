# ulmo
## A framework to scale ML model testing, back testing, and retraining.


given a raw dataset and a preprocessing script - output a curated dataset identified by dataset_id, did

given a did and an existing model run the job to produce results




website will list each available model
website shall list each preprocessed dataset

website will list jobs in progress
website will allow user to select completed job

jobs can be queued up on the command line


Given a tensorflow model and a dataset;
The dataset is loaded, and preprocessed as necessary to a form that can be directly used via model.evaluate(). Preprocessed datasets are retained by ID for future use. 

a set of related models
a set of related datasets


   
